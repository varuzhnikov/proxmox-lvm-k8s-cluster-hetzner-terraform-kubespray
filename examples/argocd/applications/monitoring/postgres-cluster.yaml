apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: database
spec:
  instances: 1

  storage:
    size: 1Gi

  bootstrap:
    initdb:
      database: app
      owner: app